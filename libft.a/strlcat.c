int len(char *str)
{
    int i;
    
    i = 0;
    while (str[i++])
    return (i);
}

char    *ft_strcat(char *src, char *dest, int n)
{
    int slen;
    int dlen;
    int i;

    slen = len(src);
    
}